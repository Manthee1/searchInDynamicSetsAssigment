#include <iostream>
#include "Test.h"

bool Test::run(enum DataStructureType testType, int keysAmount) {
	return true;
}

bool Test::run(enum DataStructureType testType, int keysAmount, int amount) {
	return true;
}
